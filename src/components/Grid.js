import styled from 'styled-components'

export default styled.div`
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  grid-gap: 1.25em;
  max-width: 76em;
  margin-left: auto;
  margin-right: auto;
`
